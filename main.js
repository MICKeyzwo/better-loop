"use strict";

const times = require("./lib/times.js");
const range = require("./lib/range.js");
const until = require("./lib/until.js");
const to = require("./lib/to.js");

module.exports = {
    times,
    range,
    until,
    to
};
